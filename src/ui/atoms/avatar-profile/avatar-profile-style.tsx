import { StyleSheet } from 'react-native';

export const AvatarProfileStyles = StyleSheet.create({
  container: {
    borderRadius: 20,
  },
  image: {
    height: 30,
    width: 30,
    borderRadius: 20,
  },
});
